# Contributing

1. Use PHP 8.2 or newer.
2. Keep the public API small and backward compatible.
3. Run `composer validate --strict` and the project test command before submitting changes.
4. Do not add application, database, framework, or local-environment dependencies.
5. Explain behavior changes and include reproducible validation steps.
